#!/usr/bin/python3

# Loop through the range of numbers from 0 to 98
for num in range(0, 99):
    # Print the decimal number and its hexadecimal representation
    print("{:d} = {}".format(num, hex(num)))
